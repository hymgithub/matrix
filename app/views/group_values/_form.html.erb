<%= form_for(@group_value) do |f| %>
  <% if @group_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_value.errors.count, "error") %> prohibited this group_value from being saved:</h2>

      <ul>
      <% @group_value.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_id %><br />
    <%= f.number_field :group_id %>
  </div>
  <div class="field">
    <%= f.label :value_id %><br />
    <%= f.number_field :value_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.number_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
