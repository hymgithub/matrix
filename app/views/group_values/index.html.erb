<h1>Listing group_values</h1>

<table>
  <tr>
    <th>Group</th>
    <th>Value</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @group_values.each do |group_value| %>
  <tr>
    <td><%= group_value.group_id %></td>
    <td><%= group_value.value_id %></td>
    <td><%= group_value.status %></td>
    <td><%= link_to 'Show', group_value %></td>
    <td><%= link_to 'Edit', edit_group_value_path(group_value) %></td>
    <td><%= link_to 'Destroy', group_value, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Group value', new_group_value_path %>
